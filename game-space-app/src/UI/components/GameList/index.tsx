import { GameListBar } from './services/GameListBar';
import { Container } from './styled';

const GameList = () => {
  return (
    <Container>
      <GameListBar />
    </Container>
  );
};

export default GameList;
